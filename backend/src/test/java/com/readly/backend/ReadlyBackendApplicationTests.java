package com.readly.backend;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Basic integration test for the Readly backend application.
 * This test verifies that the Spring Boot application context loads correctly.
 */
@SpringBootTest
class ReadlyBackendApplicationTests {

	/**
	 * Simple test to ensure the application context loads without errors.
	 */
	@Test
	void contextLoads() {
		// This test passes if Spring Boot application starts successfully
		// If there are configuration or dependency issues, this will fail
	}

}
